<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <SwitchPreference
        android:defaultValue="false"
        android:key="PREF_LOCK_FINGERPRINT"
        android:title="FingerPrint" />

    <ListPreference
        android:defaultValue="2"
        android:entries="@array/pref_lock_type_titles"
        android:entryValues="@array/pref_lock_type_values"
        android:key="PREF_LOCK_TYPE"
        android:title="@string/pref_title_lock_type" />

</PreferenceScreen>
